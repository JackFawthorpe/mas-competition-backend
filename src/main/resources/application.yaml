spring:
  application:
    name: mas-competition-backend
  datasource:
    url: jdbc:mariadb://localhost:3306/mascompetition
    driverClassName: org.mariadb.jdbc.Driver
    username: root
    password: password
  jpa:
    database-platform: org.hibernate.dialect.MariaDBDialect
    hibernate.ddl-auto: update
server:
  port: 4000